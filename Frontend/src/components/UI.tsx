import type { ReactNode } from 'react'; import { AlertCircle, FileText, RefreshCw, UploadCloud } from 'lucide-react'
export function EmptyState({title='Nothing to show yet',detail='This area will display information when it is available.'}:{title?:string;detail?:string}){return <section className="empty"><FileText size={28}/><h3>{title}</h3><p>{detail}</p></section>}
export function ErrorState({onRetry}:{onRetry?:()=>void}){return <section className="empty" role="alert"><AlertCircle size={28}/><h3>Something went wrong</h3><p>We could not retrieve this information. Try again when the service is available.</p>{onRetry&&<button className="button secondary retry" onClick={onRetry}><RefreshCw size={15}/>Try again</button>}</section>}
export function LoadingState({label='Loading information'}:{label?:string}){return <section className="loading-state" aria-live="polite" aria-label={label}><span className="skeleton skeleton-title"/><span className="skeleton"/><span className="skeleton"/><span className="skeleton skeleton-short"/></section>}
export function Field({label,children,description}:{label:string;children:ReactNode;description?:string}){return <label className="field"><span>{label}</span>{children}{description&&<small>{description}</small>}</label>}
export function UploadZone({label,onChange}:{label:string;onChange:(file?:File)=>void}){return <label className="upload"><UploadCloud size={25}/><strong>{label}</strong><span>Choose a file or drag it here</span><input type="file" accept="image/*" onChange={e=>onChange(e.target.files?.[0])}/></label>}
